'use client';

// Reuse the Medications page logic for Prescriptions as they are functionally the same in this context
import MedicationsPage from '../medications/page';

export default function PrescriptionsPage() {
    return <MedicationsPage />;
}
